const initialState = {
  articles: []
};
//returns the initial state without doing anything else
function rootReducer(state = initialState, action) {
  return state;
}
export default rootReducer;
